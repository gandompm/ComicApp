package com.example.comicapp.common

/**
 *  defining Constants as object
 *
 *  object is the singleton design pattern
 *  it only has one instance throughout the lifecycle of the application
 *  Kotlin makes a static reference for the object, and initializes the reference in a static block.
 */
object Constants {

    const val BASE_URL = "https://xkcd.com/"
}